import React from 'react';
import ChatHeaderStyle from './ChatHeader.module.scss';

const ChatHeader = () => {
    return (
        <div className={ChatHeaderStyle.ChatHeader}>

        </div>
    );
};

export default ChatHeader;