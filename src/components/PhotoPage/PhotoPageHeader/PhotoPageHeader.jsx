import React from 'react';
import PhotoPageHeaderStyle from './PhotoPageHeader.module.scss'

const PhotoPageHeader = () => {
    return (
        <div className={PhotoPageHeaderStyle.PhotoPageHeader}>
            Header
        </div>
    );
};

export default PhotoPageHeader;